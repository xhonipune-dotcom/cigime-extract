# CIGIME 3 → EDCS extract

Ky repository përdoret për të nxjerrë automatikisht rekordet e **CIGIME 3** (`CIGIME-03`) nga snapshot-i publik i EDCS në projektin `d-sanoj/EpigCorpus`.

Rezultatet do të shkruhen te dosja `output/`.
